"""
종목 추천 모델 설정 파일
- 유니버스 필터, 호라이즌 정의, 팩터 가중치를 모두 여기서 관리
- 백테스트로 가중치를 조정할 때 이 파일만 수정하면 됨
"""

# ─────────────────────────────────────────────
# 투자 호라이즌 정의 (영업일 기준)
# ─────────────────────────────────────────────
HORIZONS = {
    "short": 20,   # 단기: 약 1개월
    "mid": 60,     # 중기: 약 3개월
    "long": 120,   # 장기: 약 6개월
}

TOP_N = 5  # 추천 종목 수

# ─────────────────────────────────────────────
# 유니버스 필터 (거래 가능성 확보)
# ─────────────────────────────────────────────
UNIVERSE_FILTERS = {
    "KOSPI": {
        "min_market_cap": 100_000_000_000,     # 시총 1,000억원 이상
        "min_avg_trading_value": 500_000_000,  # 일평균 거래대금 5억원 이상
        "min_price": 1_000,                    # 동전주 제외 (원)
    },
    "KOSDAQ": {
        "min_market_cap": 50_000_000_000,      # 시총 500억원 이상
        "min_avg_trading_value": 300_000_000,  # 일평균 거래대금 3억원 이상
        "min_price": 1_000,
    },
    "NASDAQ": {
        "min_market_cap": 500_000_000,         # 시총 5억달러 이상 (USD)
        "min_avg_trading_value": 2_000_000,    # 일평균 거래대금 200만달러 이상 (USD)
        "min_price": 5,                        # 페니주 제외 (USD)
    },
}

# ─────────────────────────────────────────────
# 데이터 커버리지 페널티
# 결측 재정규화만 쓰면 팩터 1~2개만 좋고 나머지가 결측인 종목이
# 그 팩터 하나로 100% 가중치를 받아 상위권을 차지할 수 있음
# → 이 시장에서 산출 가능한 팩터 대비 유효 비율로 감점/제외
# ─────────────────────────────────────────────
COVERAGE_PENALTY = [   # (커버리지 하한, 점수 배수) — 위에서부터 첫 매칭 적용
    (0.80, 1.00),
    (0.60, 0.97),
    (0.50, 0.94),
]
MIN_COVERAGE = 0.50    # 유효 팩터 비율이 이 미만이면 추천 후보에서 제외

# ─────────────────────────────────────────────
# 호라이즌별 팩터 카테고리 가중치
# 단기 → 모멘텀/수급 중심, 장기 → 가치/퀄리티 중심
# 백테스트 검증 후 조정할 것 (초기값은 통상적 관행 기반)
# ─────────────────────────────────────────────
HORIZON_WEIGHTS = {
    "short": {
        "momentum": 0.40,
        "flow": 0.30,       # 수급 (기관/외국인/공매도) - 한국시장 위주
        "sentiment": 0.15,  # 거래량 급증 등 심리 대리변수
        "value": 0.05,
        "quality": 0.10,
    },
    "mid": {
        "momentum": 0.30,
        "flow": 0.15,
        "sentiment": 0.05,
        "value": 0.25,
        "quality": 0.25,
    },
    "long": {
        "momentum": 0.10,
        "flow": 0.05,
        "sentiment": 0.00,
        "value": 0.40,
        "quality": 0.45,
    },
}

# 종합 점수 = 각 호라이즌 점수의 가중평균
COMPOSITE_WEIGHTS = {"short": 1 / 3, "mid": 1 / 3, "long": 1 / 3}

# ─────────────────────────────────────────────
# 팩터 정의: 카테고리 → {팩터명: 방향}
# direction = +1 (높을수록 좋음) / -1 (낮을수록 좋음)
# ─────────────────────────────────────────────
FACTORS = {
    "momentum": {
        # 수익률을 변동성으로 나눈 위험조정 모멘텀: 검증 결과 점수 최상위에
        # 극단 급등주가 몰려 Top5가 평균을 하회 → 꾸준한 추세를 우대하도록 변경
        "mom_12_1_adj": +1,     # 12개월 모멘텀(최근 1개월 제외) / 변동성
        "mom_3m_adj": +1,       # 3개월 수익률 / 변동성
        "rsi_overbought": -1,   # RSI 70 초과분만 페널티 (과열 회피)
        # 주의: RSI 원값을 -1로 쓰면 'RSI 낮음=하락추세'가 고득점 되어
        #       모멘텀 팩터와 정면 충돌함 → 과열 구간 초과분만 사용
        "overheat_20d": -1,     # 최근 1개월 +20% 초과 급등분 페널티 (되돌림 위험)
    },
    "value": {
        "per": -1,          # 낮을수록 저평가
        "pbr": -1,
    },
    "quality": {
        "roe": +1,
        "debt_ratio": -1,   # 부채비율 낮을수록 좋음
        "op_margin": +1,    # 영업이익률
    },
    "flow": {
        # 순매수 '금액'을 그대로 쓰면 대형주가 항상 유리 → 시가총액 대비 비율로 보정
        "foreign_net_buy_intensity": +1,   # 외국인 20일 순매수 / 시총 (한국 전용)
        "inst_net_buy_intensity": +1,      # 기관 20일 순매수 / 시총 (한국 전용)
        "short_ratio_change": -1,          # 공매도 비중 증가 → 부정 신호
    },
    "sentiment": {
        "volume_surge": +1,  # 거래대금 급증 × 주가 방향 (상승+급증=호재, 하락+급증=악재)
    },
}

# 과열 판정 기준: 최근 20영업일 수익률이 이 값을 넘는 초과분만 감점
OVERHEAT_THRESHOLD = 0.20

# 페널티형 팩터: 값 0 = 해당 없음 (대부분의 종목)
# 일반 rank를 쓰면 0인 종목들이 동점 rank ~0.5에 몰려 카테고리 점수를
# 중앙으로 압축시킴 → 해당 없으면 카테고리 평균에서 제외하고,
# 해당하는 종목만 하위 rank로 강하게 감점
PENALTY_FACTORS = {"rsi_overbought", "overheat_20d"}

# 가치 함정(value trap) 필터: '싼데 계속 하락 중'인 종목은 가치 점수 신뢰 하락
# (저PER/저PBR 상위권인데 모멘텀 최하위 = 싼 이유가 있을 가능성)
VALUE_TRAP = {
    "value_min": 0.7,      # 가치 카테고리 rank가 이 이상이면서
    "momentum_max": 0.2,   # 모멘텀 카테고리 rank가 이 이하면
    "dampen": 0.6,         # 가치 점수에 이 배수를 곱해 감점
}

# 이상치 처리: 팩터값을 상하위 1%에서 절단 (winsorize)
WINSORIZE_PCT = 0.01
