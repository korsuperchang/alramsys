# 🏎️ 장난감 자동차 속도 측정기

핸드폰 카메라로 장난감 자동차가 지나가는 속도를 재는 웹 앱입니다.
설치할 앱도, 서버도 필요 없습니다. 브라우저에서 열고 카메라만 허용하면 됩니다.
**영상은 기기 안에서만 처리되며 어디로도 전송되지 않습니다.**

측정 방식은 두 가지이고, 화면에서 바로 바꿀 수 있습니다.

| | 자동 추적 (기본) | 기준선 2개 |
| --- | --- | --- |
| 준비 | 폰만 고정 | 바닥 두 지점 거리를 자로 재고 선을 맞춤 |
| 결과 | 상대 속도(초당 화면폭 배수), 화면 가로 길이를 넣으면 km/h | km/h |
| 강점 | 작은 차·먼 거리에서도 잘 잡힘 | 값이 더 정확함 |

## 자동 추적 원리

1. **몇 프레임 전과 달라진 곳**을 찾습니다(프레임 간 차분, 기본 2프레임 전).
   오래 쌓아 둔 배경과 비교하는 방식은 카메라가 조금씩 미끄러지기만 해도 바닥 무늬
   전체가 달라져서, 실제 촬영에서는 자동차가 그 속에 묻힙니다.
2. 진행 축 방향 분포에서 **가장 두꺼운 덩어리 하나**만 골라냅니다.
   바닥 무늬에서 오는 잔 노이즈는 화면 전체에 얇게 퍼지지만 자동차는 한곳에 뭉칩니다.
3. 다음 프레임의 덩어리가 **보던 물체의 다음 위치로 볼 수 있는 자리인지** 확인합니다.
   화면 한쪽에서 손이 움직이다가 반대쪽에 자동차가 나타나면 다른 물체로 보고 통과를 끊습니다.
   (이걸 안 하면 손과 자동차가 한 통과로 묶여 궤적이 뒤죽박죽이 되고, 아무것도 측정되지 않습니다.)
4. 그 덩어리의 중심을 매 프레임 기록하고, 통과가 끝나면 위치-시간 표본에
   **최소제곱 직선**을 맞춰 기울기를 속도로 씁니다. 크게 벗어나는 표본은 버리고 다시
   맞추기 때문에, 통과 앞뒤에 그림자 같은 잔 움직임이 한둘 끼어도 측정을 놓치지 않습니다.

- 얇은 선 위에서 정해진 면적을 가려야 하는 문턱이 없어서, 멀리서 찍은 작은 자동차도 잡힙니다.
- 속도는 **초당 화면 폭의 몇 배**(fwps)로 나옵니다. 카메라를 고정해 두면 이 값끼리 그대로 비교되고,
  화면에 담긴 실제 가로 길이를 넣으면 m/s·km/h로 환산됩니다.
- 직선 적합도(R²)와 최소 이동 거리로 갔다 왔다 하는 움직임이나 그림자를 걸러냅니다.
  가속·감속하거나 경로가 휘어도 괜찮습니다. 그 구간의 **평균 속도**로 나옵니다.

## 손떨림 보정

손으로 든 폰은 화면 전체가 매 프레임 조금씩 밀립니다. 그대로 두면 바닥 무늬가 통째로
"움직이는 것"으로 잡혀서, 자동 추적은 아무것도 못 잡고 기준선은 헛측정을 쏟아냅니다.
(측정해 보니 **±1픽셀 흔들림만으로도** 자동 추적은 0건, 기준선은 방향이 뒤죽박죽인 가짜 측정 7건이 나왔습니다.)

그래서 프레임마다 가로·세로 밝기 합(투영)을 구하고 직전 프레임과 가장 잘 겹치는 이동량을 찾아,
그만큼 어긋난 자리끼리 비교합니다. 1차원 두 개만 보므로 값이 싸고, 손떨림은 대부분 평행 이동이라
이것으로 충분합니다. 오차를 합칠 때 큰 값부터 30%를 버려서 **지나가는 자동차를 화면 흔들림으로 착각하지 않고**,
가만히 있는 쪽에 유리하게 판정해서 **타일처럼 반복되는 바닥 무늬에 속지 않습니다**.

| 흔들림 (처리 해상도 200px 기준) | 자동 추적 | 기준선 2개 |
| --- | --- | --- |
| ±1 ~ ±4px | 오차 10% 이내로 측정 | 정상 측정 |
| ±8px | 오차 10% 이내로 측정 | 헛측정 대신 침묵 |
| ±12px 이상 | 측정을 멈추고 "흔들림" 표시 | 측정을 멈춤 |

느리게 미끄러지는 카메라(2초에 8px)도 같은 방식으로 따라잡습니다. 이동량을 직전
프레임이 아니라 **기준 화면과 직접** 견주기 때문입니다. 프레임 간 비교는 1픽셀 미만의
느린 드리프트를 매번 0으로 반올림해 놓치고, 그게 쌓이면 화면 전체가 어긋납니다.

즉 **가볍게 든 손 정도는 보정되지만, 크게 흔들리면 틀린 값을 내는 대신 측정을 멈춥니다.**
그래도 고정할 수 있으면 고정하는 편이 가장 정확합니다.

## 기준선 2개 원리

화면에 **기준선(게이트) 두 개**를 세워 놓고, 자동차가 각 선을 가리는 순간의 시각을 잡습니다.

```
    A선            B선
     │              │        v = d / Δt
  ───┼──────────────┼───     d  = 두 선 사이의 실제 거리 (자로 직접 측정)
     │   🚗 →       │        Δt = A선 통과 → B선 통과 시간
     │←──── d ─────→│
```

- 배경 차분(background subtraction)으로 "배경과 밝기가 달라진 픽셀"의 비율을 각 선에서 계속 계산합니다.
- 그 비율이 임계값을 넘는 순간이 통과 시점이고, **앞뒤 프레임 사이를 선형 보간**해서 프레임 단위(30fps면 33ms)보다 더 정밀하게 시각을 추정합니다.
- 형태 추적이 아니라 "선을 가렸는가"만 보기 때문에, 자동차 색·모양·조명에 둔감하고 저사양 폰에서도 잘 돕니다.
- 두 게이트 사이를 **실제로 지나왔는지**(가운데 구간의 프레임 간 움직임)까지 확인한 짝만 측정으로 인정합니다.
  이 확인이 없으면 게이트를 한 번 놓쳤을 때 짝이 어긋난 채 고정되어, 이후 측정이 "한 바퀴 도는 시간"으로 잡히고 방향까지 뒤집힙니다.

거리 `d`를 사람이 직접 재서 넣기 때문에, 카메라 캘리브레이션 없이도 물리적으로 정확한 값이 나옵니다.

## 쓰는 법

1. 바닥에 자동차가 지나갈 **직선 구간**을 정하고, 두 지점에 테이프를 붙인 뒤 그 사이 거리를 자로 잽니다. (30~100cm 권장)
2. 앱에 그 거리를 입력합니다.
3. **핸드폰을 고정**하고(삼각대·책·컵 무엇이든), 주행 경로를 옆에서 정면으로 바라보게 둡니다.
4. 화면의 A·B 선을 드래그해서 바닥 표시 지점에 맞춥니다.
5. "배경 학습 중" 표시가 사라지면 자동차를 굴립니다. 두 선을 지나는 순간 속도가 자동으로 뜹니다.

측정값은 km/h, m/s, cm/s로 표시되고 기록이 자동 저장됩니다(최근 50건). `1:64` 같은 축척을 설정하면 "실제 차라면 몇 km/h인지" 환산값도 같이 보여줍니다.

카메라가 없거나 데스크톱에서 동작만 확인하고 싶다면 **데모 모드**를 누르세요. 가상의 자동차가 화면을 지나가며 전체 파이프라인이 그대로 돌아갑니다.

## 정확하게 재는 요령

| 상황 | 대처 |
| --- | --- |
| 값이 들쭉날쭉하다 | 흔들림이 보정 범위를 넘고 있습니다. 화면 안내에 보정량이 픽셀로 표시되니 확인하고, 폰을 어딘가에 기대세요. |
| 자동차가 화면 어디로 지나가야 하나 | 아무 데나 괜찮습니다. 가운데일 필요도, 곧게 갈 필요도 없습니다. 가속·감속해도 됩니다(그 구간의 평균 속도로 나옵니다). |
| 측정이 안 되는데 이유를 모르겠다 | 화면 안내를 보세요. 표본 부족·이동 거리 부족·불규칙한 움직임 중 어느 것인지 알려 줍니다. |
| 자동 추적이 아무것도 못 잡는다 | 계기판의 **최근 최대**가 **필요**보다 작으면 민감도를 올리세요. 그래도 0.1% 아래면 자동차가 화면에서 너무 작습니다. |
| 아예 감지가 안 된다 (기준선 방식) | **자동 맞춤**을 누르세요. 그래도 "최대 반응"이 1% 아래면 자동차가 화면에서 너무 작습니다 — 카메라를 더 가까이 두거나 감지 구간을 좁히세요. |
| 차가 너무 빨라 그냥 지나간다 | **기준선 두께**를 올리세요. 얇은 선은 프레임 사이에 건너뛸 수 있습니다. |
| 두 기준선이 동시에 계속 켜져 있다 | 폰이 흔들리고 있습니다. 안내에도 그렇게 뜹니다. 고정하세요. |
| 가만히 있는데 계속 잡힌다 | 민감도를 내리세요. 그림자·나뭇잎·형광등 깜빡임이 원인일 수 있습니다. |
| 오차 범위(±)가 크다 | 두 선의 간격을 넓히세요. 통과 시간이 길어질수록 프레임 양자화 오차 비중이 줄어듭니다. |
| 아주 빠른 차 | 60fps 이상을 지원하는 카메라가 유리합니다. 화면에 표시되는 fps를 확인하세요. |

표시되는 `± x.xx km/h`는 프레임 주기에서 오는 시간 분해능 한계를 보수적으로 환산한 값입니다.
(예: 30fps에서 Δt가 250ms면 약 ±13%, Δt가 1초면 약 ±3%)

카메라는 정면(경로에 수직)에서 보는 것이 좋습니다. 비스듬히 보면 두 기준선이 바닥에서 실제로 어디를 가리키는지 어긋나서 계통 오차가 생깁니다.

## 실행

카메라 API는 **HTTPS 또는 localhost**에서만 동작합니다.

```bash
# 로컬에서 확인
cd toycar-speed
npm run serve          # http://localhost:8000
```

### 파일 하나로 쓰기

`standalone.html` 은 CSS·JS가 전부 인라인된 단일 파일이라, 그 파일 하나만 어디에 올려도 그대로 돕니다.
소스는 `index.html` / `css` / `js` 쪽 하나뿐이고, 아래 명령으로 다시 만듭니다.

```bash
npm run build          # standalone.html + dist/artifact.html 생성
```

핸드폰에서 쓰려면 HTTPS로 올려야 합니다. GitHub Pages가 가장 간단합니다:
저장소 **Settings → Pages → Source: Deploy from a branch** 에서 브랜치를 고르면
`https://<사용자>.github.io/<저장소>/toycar-speed/` 로 접속할 수 있고,
브라우저 메뉴의 "홈 화면에 추가"를 누르면 앱처럼 전체 화면으로 실행됩니다(PWA, 오프라인 동작).

## 실제 촬영으로 확인한 것

퍼즐 매트 위를 지나가는 장난감 자동차를 폰으로 찍은 1.9초짜리 영상(29.97fps)을 그대로
엔진에 넣어 확인했습니다. 처음에는 아무것도 측정되지 않았는데, 원인은 자동차가 아니라
**카메라가 2초 동안 8픽셀쯤 미끄러진 것**이었습니다. 매트의 촘촘한 무늬가 통째로
"움직임"으로 잡혀 화면의 7%가 반응했고, 자동차(1.5%)는 그 속에 묻혔습니다.

프레임 간 차분 + 덩어리 골라내기 + 이상치를 버리는 직선 적합으로 바꾼 뒤, 같은 영상에서
`1.72 화면폭/초 (오른쪽→왼쪽, 표본 14개, R²=0.99)`로 측정됩니다.

이어서 받은 영상 두 개로 두 가지를 더 잡았습니다.

| 영상 | 증상 | 원인 | 결과 |
| --- | --- | --- | --- |
| 자동차를 손으로 굴려 보낸 영상 | 아무것도 측정 안 됨 | 화면 왼쪽의 손 움직임과 오른쪽의 자동차가 **한 통과로 묶임** | `1.71 화면폭/초` (이동 91%, R²=1.00) |
| 카메라를 따라 움직인 영상 | 값이 둘로 갈림 | 카메라가 크게 움직인 8프레임 동안 아무것도 못 봄 | 측정하되 **"카메라가 움직였다"고 표시** |

각 상황을 합성 프레임으로 재현한 회귀 테스트가 들어 있습니다.

## 테스트

감지 엔진은 DOM에 의존하지 않는 순수 모듈이라 합성 프레임으로 검증합니다.

```bash
npm test
```

지나가는 차의 픽셀 속도로부터 기대되는 Δt와 실제 측정값이 프레임 주기 이내로 일치하는지,
정지 화면(노이즈 포함)에서 오검출이 없는지, 방향·가로 게이트·연속 측정이 맞는지,
게이트를 놓쳐 짝이 어긋났을 때 스스로 바로잡는지,
그리고 카메라가 흔들리는 상황을 재현해 값이 흔들리지 않는지 확인합니다.

## 파일 구조

```
toycar-speed/
├── index.html              화면 구성
├── css/styles.css          스타일 (다크, 모바일 우선)
├── js/stabilizer.js        손떨림 추정 (투영 정합, DOM 비의존)
├── js/tracker.js           자동 추적 엔진 (DOM 비의존, 테스트 대상)
├── js/detector.js          기준선 감지 엔진 (DOM 비의존, 테스트 대상)
├── js/app.js               카메라·오버레이·기록·설정
├── test/stabilizer.test.mjs 손떨림 보정 테스트 (흔들리는 카메라 재현 포함)
├── test/tracker.test.mjs   자동 추적 테스트
├── test/detector.test.mjs  기준선 감지 테스트
├── build-standalone.mjs    단일 파일 빌드 (인라인만 수행)
├── standalone.html         빌드 결과 — 이 파일 하나로 실행 가능
├── sw.js                   오프라인 캐시
└── manifest.webmanifest    홈 화면 추가용 PWA 설정
```
